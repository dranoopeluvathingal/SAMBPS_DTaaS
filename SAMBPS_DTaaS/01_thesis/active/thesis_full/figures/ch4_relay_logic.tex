% ch4_relay_logic.tex — Hierarchical vote logic flowchart (C1–C5 suite)
\begin{tikzpicture}[
  every node/.style={font=\scriptsize},
  blk/.style={block, minimum width=2.8cm, minimum height=0.7cm},
  and/.style={draw=thesisblue, thick, fill=lightblue!40, circle,
              minimum size=0.55cm, inner sep=0pt, font=\tiny},
  or/.style={draw=thesisgreen, thick, fill=lightgreen!40, circle,
             minimum size=0.55cm, inner sep=0pt, font=\tiny},
]

%% Input phasors
\node[blk, fill=gray!10] (IN) at (0,0)
  {Input: $\phasor{I}_A$, $\phasor{I}_B$, $\phasor{V}_{abc}$};

%% Four elements
\node[blk, draw=thesisblue]   (E1) at (-4.5,-2.0) {T1: Adaptive 87L\\$k_m^\ast(\kibr)$};
\node[blk, draw=thesisorange] (E2) at (-1.5,-2.0) {T2: Harmonic\\87LH};
\node[blk, draw=thesisgreen]  (E3) at ( 1.5,-2.0) {T3: Neg-seq\\87LN};
\node[blk, draw=thesisgray]   (E4) at ( 4.5,-2.0) {T4: TDCS\\Transient};

\foreach \e in {E1,E2,E3,E4}
  \draw[arr] (IN.south) -- (\e.north);

%% Physics veto gate
\node[blk, draw=thesisgray!70, fill=gray!10, minimum width=2.2cm] (VET)
  at (0,-4.2) {Physics Veto\\$\hat{\kibr}\!\in\![0,2],\;\hat{\alpha}\!\in\![0,1]$};

%% OR gate over T1–T4
\node[or] (OR) at (0,-5.8) {$\vee$};
\foreach \e in {E1,E2,E3,E4}
  \draw[arr, thesisblue] (\e.south) -- ++(0,-1.4) -| (OR.north);

%% AND gate: OR result ∧ veto
\node[and] (AND) at (0,-7.2) {$\wedge$};
\draw[arr, thesisblue]   (OR.south)  -- (AND.north);
\draw[arr, thesisgray]   (VET.south) -- ++(0,-0.8) -| (AND.east);

%% Blocking condition
\node[blk, draw=thesisred, fill=red!10, minimum width=2.2cm] (BLOCK)
  at (-4.0,-7.2) {Block: $I_c/I_\mathrm{bias} > 0.10$};
\draw[arrrev, thesisred, thick] (BLOCK.east)
  -- node[above, font=\tiny]{inhibit} (AND.west);

%% Trip output
\node[blk, fill=thesisred!20, draw=thesisred, minimum width=2.2cm] (TRIP)
  at (0,-8.8) {TRIP OUTPUT\\$T \in \{0, 1\}$};
\draw[arr, thesisred, very thick] (AND.south) -- (TRIP.north);

%% Coverage annotations
\node[annotbox] at (-4.5,-3.0) {\tiny All faults\\nominal $\kibr$};
\node[annotbox] at (-1.5,-3.0) {\tiny IBR harmonic\\faults};
\node[annotbox] at ( 1.5,-3.0) {\tiny Asymmetric\\IBR faults};
\node[annotbox] at ( 4.5,-3.0) {\tiny 3P IBR-only\\lines};

\end{tikzpicture}
