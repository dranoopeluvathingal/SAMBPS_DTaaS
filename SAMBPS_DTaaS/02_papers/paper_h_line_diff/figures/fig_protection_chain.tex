% fig_protection_chain.tex — fragment (\input-able, no standalone wrapper)
\definecolor{elemBlue}  {RGB}{31,  119, 180}
\definecolor{gateOrange}{RGB}{214, 100,  14}
\definecolor{tripRed}   {RGB}{180,  30,  30}
\tikzset{
  elem/.style   = {rectangle, rounded corners=3pt, draw=elemBlue, fill=elemBlue!12,
                   line width=0.9pt, text width=2.6cm, align=center,
                   minimum height=0.85cm, font=\small},
  gate/.style   = {rectangle, rounded corners=3pt, draw=#1, fill=#1!12,
                   line width=0.9pt, text width=1.2cm, align=center,
                   font=\small\bfseries},
  arr/.style    = {-{Stealth[length=4.5pt]}, line width=0.85pt, color=gray!70!black},
  garr/.style   = {-{Stealth[length=4.5pt]}, line width=0.85pt, color=#1},
  lbl/.style    = {font=\scriptsize, align=center, color=gray!55!black},
}
\begin{tikzpicture}[node distance=0.45cm and 1.6cm]

% ---- Protection element blocks (column 1) -----------------------------------
\node[elem] (e87L)  {87L\\{\scriptsize\color{gray}Fundamental diff.}};
\node[elem, below=of e87L]  (e87LN)  {87LN\\{\scriptsize\color{gray}Neg.-seq. diff.}};
\node[elem, below=of e87LN] (e87LN0) {87LN0\\{\scriptsize\color{gray}Zero-seq. diff.}};
\node[elem, below=of e87LN0](eTDCS)  {TDCS-87L\\{\scriptsize\color{gray}TDCS scalar}};

% ---- OR gate (column 2), centred on the 4 elements -------------------------
\node[gate=elemBlue, minimum height=4.8cm, text width=1.1cm,
      right=1.6cm of $(e87L.east)!0.5!(eTDCS.east)$] (OR)
  {\textbf{OR}\\[4pt]$\bigvee$};

% ---- AND gate (column 3), centred on OR ------------------------------------
\node[gate=tripRed, minimum height=1.8cm, text width=1.1cm,
      right=1.8cm of OR] (AND)
  {\textbf{AND}\\[4pt]$\bigwedge$};

% ---- gamma gate block — directly below AND ----------------------------------
\node[gate=gateOrange, minimum height=0.9cm, text width=2.4cm,
      below=1.0cm of AND] (gamma)
  {$\gamma$-gate\\{\scriptsize IBR confidence}};

% ---- TRIP output label -------------------------------------------------------
\node[right=1.4cm of AND, font=\large\bfseries, color=tripRed] (trip) {TRIP};

% ---- Arrows: elements → OR --------------------------------------------------
\draw[garr=elemBlue] (e87L.east)  -- (e87L.east  -| OR.west);
\draw[garr=elemBlue] (e87LN.east) -- (e87LN.east -| OR.west);
\draw[garr=elemBlue] (e87LN0.east)-- (e87LN0.east-| OR.west);
\draw[garr=elemBlue] (eTDCS.east) -- (eTDCS.east -| OR.west);

% ---- Arrow: OR → AND --------------------------------------------------------
\draw[garr=elemBlue] (OR.east) -- (AND.west);

% ---- Arrow: gamma → AND (straight up) ----------------------------------------
\draw[garr=gateOrange] (gamma.north) --
  node[right, lbl, xshift=2pt]{$\gamma \geq \gamma_{th}$} (AND.south);

% ---- Arrow: AND → TRIP ------------------------------------------------------
\draw[garr=tripRed, line width=1.2pt]
  (AND.east) -- (trip.west);

% ---- Gamma threshold annotation ----------------------------------------------
\node[font=\scriptsize, color=gateOrange!80!black, align=center,
      anchor=north] at ($(gamma.south) + (0, -0.15)$)
  {$\gamma_{th} = 0.80$;\enspace $k_{ibr} \geq 0.06$};

% ---- Group box: protection elements -----------------------------------------
\begin{scope}[on background layer]
  \node[rectangle, rounded corners=4pt, draw=elemBlue!50, fill=elemBlue!5,
        dashed, line width=0.7pt,
        fit=(e87L)(e87LN)(e87LN0)(eTDCS), inner sep=8pt] (elembox) {};
\end{scope}

% ---- "Protection elements" label — below TDCS box (concrete ref) ------------
\node[font=\scriptsize\bfseries, color=elemBlue!75!black,
      anchor=north west] at ($(eTDCS.south west) + (0, -0.22)$)
  {Protection elements};

% ---- Title — centred above OR gate top, with generous clearance --------------
\node[font=\small\bfseries, color=black!70,
      anchor=south] at ($(OR.north) + (0, 0.55)$)
  {IBR-Adaptive 87L Protection Chain};

\end{tikzpicture}
