UPSTREAM LICENSE — TO BE REPLACED AT VENDOR TIME
================================================

The canonical Wang-2020 distortion-controllable HIAF (DistC-HIAF)
PSCAD model is open-source.  The upstream repository

    https://github.com/MingjieWei/PSCAD-FILE-DISTC-HIAF-Model

carries the project's LICENSE (verbatim copy to be placed here at
vendor time, replacing this placeholder file).

Until the upstream files are vendored under the SAMBPS-DTaaS
permitted-vendoring policy on the lead engineer's licensed Windows
runner, this placeholder exists ONLY to:

* signal that the directory is reserved for upstream third-party
  code (i.e., do NOT add SAMBPS-DTaaS sources here);
* document the upstream URL for review during the next
  vendor-policy compliance pass.

When the upstream license is verified and the sources vendored,
this `LICENSE.placeholder` file will be REPLACED by the verbatim
upstream `LICENSE` (no modifications, attribution preserved), per
the SAMBPS-DTaaS permitted-vendoring policy.
