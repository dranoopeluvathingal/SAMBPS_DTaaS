"""hil — Hardware-in-the-loop tests.

WP5.2 ``test_latency.py`` lives here.  Tests are kept separate
from the ``tests/`` tree because they require either a HIL
testbed (HW mode) or the dev-box mock-replay path (default); the
``test_latency.py`` module documents both.
"""
